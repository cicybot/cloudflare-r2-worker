name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - product

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'
      - name: Install wrangler CLI
        run: |
          npm install -g wrangler
          npm install -d
      - name: Publish Worker to Cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_STORAGE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_STORAGE_ACCOUNT_ID }}
        run: |
          export
          npm run deploy
